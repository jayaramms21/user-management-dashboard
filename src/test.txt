Question 1 – User Management Dashboard
Simple Step-by-Step Instructions
Create React Project

Open terminal:

npm create vite@latest user-management-dashboard

Choose:

Framework → React

Variant → JavaScript

Run project:

cd user-management-dashboard
npm install
npm run dev

Open browser and confirm:

http://localhost:5173

STEP 2: Create Folder Structure

Inside src/ create folders and files:

src/
├── components/
│ ├── SearchBar.jsx
│ ├── UserTable.jsx
│ └── Loader.jsx
├── services/
│ └── api.js
├── pages/
│ └── Home.jsx
├── App.jsx
└── main.jsx

Delete:

App.css

index.css (optional)
STEP 3: Add Bootstrap (for table & spinner)
npm install bootstrap

In main.jsx:

import 'bootstrap/dist/css/bootstrap.min.css';

STEP 4: Create API Service

src/services/api.js

const API_URL = "https://python.sicsglobal.com/userdetails_api/users";

export const fetchUsers = async () => {
const response = await fetch(API_URL);
if (!response.ok) {
throw new Error("Failed to fetch users");
}
return response.json();
};

✔ API logic separated
✔ Clean code

STEP 5: Create Loader Component

src/components/Loader.jsx

export default function Loader() {
return (
<div className="text-center mt-5">
<div className="spinner-border" role="status"></div>
<p>Loading...</p>
</div>
);
}

✔ Shows loading spinner

STEP 6: Create SearchBar Component

src/components/SearchBar.jsx

export default function SearchBar({ search, setSearch }) {
return (
<input
type="text"
className="form-control mb-3"
placeholder="Search by name..."
value={search}
onChange={(e) => setSearch(e.target.value)}
/>
);
}

✔ Search by name
✔ Controlled input

STEP 7: Create UserTable Component

src/components/UserTable.jsx

export default function UserTable({ users, onDelete }) {
return (
<table className="table table-bordered">
<thead className="table-dark">
<tr>
<th>Name</th>
<th>Email</th>
<th>City</th>
<th>Company</th>
<th>Actions</th>
</tr>
</thead>

      <tbody>
        {users.length === 0 ? (
          <tr>
            <td colSpan="5" className="text-center">
              No users found
            </td>
          </tr>
        ) : (
          users.map((user) => (
            <tr key={user.id}>
              <td>{user.name}</td>
              <td>{user.email}</td>
              <td>{user.city}</td>
              <td>{user.company}</td>
              <td>
                <button
                  className="btn btn-danger btn-sm"
                  onClick={() => onDelete(user.id)}
                >
                  Delete
                </button>
              </td>
            </tr>
          ))
        )}
      </tbody>
    </table>

);
}

✔ Required columns
✔ Delete (UI only)

STEP 8: Create Home Page (Main Logic)

src/pages/Home.jsx

import { useEffect, useState } from "react";
import { fetchUsers } from "../services/api";
import SearchBar from "../components/SearchBar";
import UserTable from "../components/UserTable";
import Loader from "../components/Loader";

export default function Home() {
const [users, setUsers] = useState([]);
const [search, setSearch] = useState("");
const [loading, setLoading] = useState(true);
const [error, setError] = useState("");

useEffect(() => {
loadUsers();
}, []);

const loadUsers = async () => {
try {
const data = await fetchUsers();
setUsers(data);
} catch (err) {
setError(err.message);
} finally {
setLoading(false);
}
};

const handleDelete = (id) => {
setUsers(users.filter((user) => user.id !== id));
};

const filteredUsers = users.filter((user) =>
user.name.toLowerCase().includes(search.toLowerCase())
);

if (loading) return <Loader />;
if (error) return <p className="text-danger text-center">{error}</p>;

return (
<div className="container mt-5">
<h3 className="mb-4">User Management Dashboard</h3>
<SearchBar search={search} setSearch={setSearch} />
<UserTable users={filteredUsers} onDelete={handleDelete} />
</div>
);
}

✔ useEffect
✔ Loading & error
✔ Search
✔ Delete

STEP 9: Update App.jsx

src/App.jsx

import Home from "./pages/Home";

export default function App() {
return <Home />;
}

STEP 10: Run & Test
npm run dev

Check:
✔ API loads
✔ Search works
✔ Delete removes row
✔ Spinner shows
✔ Error handled

GIT PUSH STEPS (SAME SIMPLE WAY)
STEP 11: Create GitHub Repository

Go to GitHub

Create new repo

Name: user-management-dashboard

Do NOT add README

STEP 12: Add README.md

Create README.md in project root:

# User Management Dashboard

## Features

- Fetch users from API
- Search users by name
- Delete users (UI only)
- Loading spinner
- Error handling

## Tech Stack

- React (Vite)
- Hooks (useState, useEffect)
- Bootstrap

## Run Project

npm install
npm run dev

STEP 13: Push Code to GitHub

Run commands one by one:

git init
git add .
git commit -m "User Management Dashboard using React Hooks"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main

FINAL ASSESSMENT READY CHECKLIST

✔ Functional components only
✔ Hooks used
✔ Correct API
✔ Clean structure
✔ GitHub pushed

If you want, next I can:

Explain how to explain this in interview

Give exam time tips

Tell what assessors usually check
----------------------------------------------
(React + TypeScript) – Simple Steps
🟢 STEP 1: Create React + TypeScript Project

Open terminal:

npm create vite@latest user-management-dashboard


Choose:

Framework → React

Variant → TypeScript

Run project:

cd user-management-dashboard
npm install
npm run dev


Confirm app runs at:

http://localhost:5173

 STEP 2: Folder Structure

Inside src/ create:

src/
 ├── components/
 │    ├── SearchBar.tsx
 │    ├── UserTable.tsx
 │    └── Loader.tsx
 ├── services/
 │    └── api.ts
 ├── pages/
 │    └── Home.tsx
 ├── App.tsx
 └── main.tsx


Delete (optional):

App.css

index.css

STEP 3: Install Bootstrap
npm install bootstrap


In main.tsx:

import 'bootstrap/dist/css/bootstrap.min.css';

STEP 4: API Service (TypeScript)

src/services/api.ts

const API_URL = "https://python.sicsglobal.com/userdetails_api/users";

export interface User {
  id: number;
  name: string;
  email: string;
  city: string;
  company: string;
}

export const fetchUsers = async (): Promise<User[]> => {
  const response = await fetch(API_URL);
  if (!response.ok) {
    throw new Error("Failed to fetch users");
  }
  return response.json();
};


✔ Proper typing
✔ Clean separation

STEP 5: Loader Component

src/components/Loader.tsx

export default function Loader() {
  return (
    <div className="text-center mt-5">
      <div className="spinner-border" role="status"></div>
      <p>Loading...</p>
    </div>
  );
}

STEP 6: SearchBar Component

src/components/SearchBar.tsx

interface Props {
  search: string;
  setSearch: (value: string) => void;
}

export default function SearchBar({ search, setSearch }: Props) {
  return (
    <input
      type="text"
      className="form-control mb-3"
      placeholder="Search by name..."
      value={search}
      onChange={(e) => setSearch(e.target.value)}
    />
  );
}


✔ Typed props
✔ Controlled input

 STEP 7: UserTable Component

src/components/UserTable.tsx

import { User } from "../services/api";

interface Props {
  users: User[];
  onDelete: (id: number) => void;
}

export default function UserTable({ users, onDelete }: Props) {
  return (
    <table className="table table-bordered">
      <thead className="table-dark">
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>City</th>
          <th>Company</th>
          <th>Actions</th>
        </tr>
      </thead>

      <tbody>
        {users.length === 0 ? (
          <tr>
            <td colSpan={5} className="text-center">
              No users found
            </td>
          </tr>
        ) : (
          users.map((user) => (
            <tr key={user.id}>
              <td>{user.name}</td>
              <td>{user.email}</td>
              <td>{user.city}</td>
              <td>{user.company}</td>
              <td>
                <button
                  className="btn btn-danger btn-sm"
                  onClick={() => onDelete(user.id)}
                >
                  Delete
                </button>
              </td>
            </tr>
          ))
        )}
      </tbody>
    </table>
  );
}


✔ Required columns
✔ UI delete only
✔ Type safety

STEP 8: Home Page (Main Logic)

src/pages/Home.tsx

import { useEffect, useState } from "react";
import { fetchUsers, User } from "../services/api";
import SearchBar from "../components/SearchBar";
import UserTable from "../components/UserTable";
import Loader from "../components/Loader";

export default function Home() {
  const [users, setUsers] = useState<User[]>([]);
  const [search, setSearch] = useState("");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");

  useEffect(() => {
    loadUsers();
  }, []);

  const loadUsers = async () => {
    try {
      const data = await fetchUsers();
      setUsers(data);
    } catch (err: any) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  const handleDelete = (id: number) => {
    setUsers(users.filter((user) => user.id !== id));
  };

  const filteredUsers = users.filter((user) =>
    user.name.toLowerCase().includes(search.toLowerCase())
  );

  if (loading) return <Loader />;
  if (error) return <p className="text-danger text-center">{error}</p>;

  return (
    <div className="container mt-5">
      <h3 className="mb-4">User Management Dashboard</h3>
      <SearchBar search={search} setSearch={setSearch} />
      <UserTable users={filteredUsers} onDelete={handleDelete} />
    </div>
  );
}

 STEP 9: App.tsx

src/App.tsx

import Home from "./pages/Home";

export default function App() {
  return <Home />;
}

STEP 10: Run & Verify
npm run dev


Check:
✔ API loads
✔ Spinner shows
✔ Search works
✔ Delete works
✔ No TypeScript errors

GIT PUSH STEPS (SIMPLE)
 STEP 11: Create GitHub Repo

Repository name: user-management-dashboard

Do NOT add README while creating

 STEP 12: Add README.md

Create README.md:

# User Management Dashboard (TypeScript)

## Features
- Fetch users from API
- Search by name
- Delete users (UI only)
- Loading spinner
- Error handling

## Tech Stack
- React (Vite)
- TypeScript
- Hooks
- Bootstrap

## Run Project
npm install
npm run dev

 STEP 13: Push to GitHub
git init
git add .
git commit -m "User Management Dashboard - React TypeScript"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main

FINAL CHECK (ASSESSMENT READY)

✔ React + TypeScript
✔ Functional components only
✔ Hooks used
✔ Correct API
✔ GitHub pushed